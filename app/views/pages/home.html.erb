<% if logged_in? %>
  <%= link_to 'New Critter', new_critter_path, :class => 'button right' %>
<% end %>
<div class="clearfix"></div>
<%= render 'shared/top_critters' %>
<%= render 'shared/recent_critters' %>
